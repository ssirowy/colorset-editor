import * as React from 'react'

// Lens components
import {
  Button,
  Heading
} from 'looker-lens'

export const Header = () => (
  <header>
    <Heading>Colorset editor</Heading>
    <Button>Copy JSON</Button>
  </header>
)
